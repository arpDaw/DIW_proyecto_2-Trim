import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'proyecto_DIW',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
